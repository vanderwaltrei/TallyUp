<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyApplication" parent="Base.Theme.MyApplication" />

    <style name="CustomDatePicker" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <item name="android:datePickerStyle">@style/CustomDatePickerWidget</item>
    </style>

    <style name="CustomDatePickerWidget" parent="android:Widget.Material.DatePicker">
        <item name="android:headerBackground">@color/primary</item>
        <item name="android:calendarTextColor">@color/primary_foreground</item>
        <item name="android:dateTextAppearance">@style/DateTextStyle</item>
    </style>

    <style name="DateTextStyle" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textColor">@color/primary</item>
    </style>
</resources>